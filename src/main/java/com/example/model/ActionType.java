package com.example.model;

/**
 * Distinguishes actions the operator can take now from follow-up collection steps.
 */
public enum ActionType {
    IMMEDIATE,
    INVESTIGATION,
    DATA_COLLECTION
}
